//Calcular área de um triângulo

function area(base, altura) {
    let resultado = (base * altura) / 2 
    console.log(`A área do triangulo é igual a: ${resultado} cm²`)
}

module.exports = area;